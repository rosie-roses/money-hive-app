"use server";

export const signIn = async ({ email, password }: signInProps) => {
    try {
        // Mutation / Database / Make fetch
    } catch (error) {
        console.error('Error', error);
    }
}

export const signUp = async (userDate: SignUpParams) => {
    try {
        // Create a user account
    } catch (error) {
        console.error('Error', error);
    }
}